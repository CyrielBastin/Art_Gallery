{% extends 'base.html.twig' %}

{% trans_default_domain 'painting' %}

{% block title %}{% trans %}Edit your commentary{% endtrans %}{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center title-form-custom">{% trans %}Edit your comment{% endtrans %}</h1>
        {{ form_start(form) }}
        <div class="d-flex justify-content-center">
            {{ form_row(form.commentary, { 'label': app.user.userprofile.pseudo ~ ' :', 'label_attr': { 'style': 'font-size: 2em;' }, 'attr': { 'style': 'width: 500px; height: 180px;' } }) }}
        </div>
        <div class="d-flex justify-content-center">
            {{ form_row(form.submit, { 'label': 'Edit', 'attr': { 'class': 'btn btn-info btn-lg', 'style': 'width: 200px; height: 50px; font-size: 1.8em; padding-bottom: 50px;' } }) }}
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
